node_modules/
dist/
*.log
.DS_Store
.vitest/
coverage/
launch-extension/package-*.zip
*.tgz

# The marketing site loads this built copy of the library (regenerated by
# `npm run build`). It is committed so the site can be deployed as a static
# bundle without a build step.
site/*.png
